export default interface sass {
    /**
     *支持 sass，值为 node-sass 的配置参数。

注意：开启 sass 支持需在项目代码中安装 node-sass 和 sass-loader 两个依赖。
     *
     * @type {*}
     * @memberof sass
     */
    sass?: any;
}
